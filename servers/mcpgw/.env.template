# Base URL of the MCP Gateway Registry
REGISTRY_BASE_URL=http://localhost

# Timeout for registry API calls in seconds (default: 30)
REGISTRY_API_TIMEOUT=30

# Port for this MCP server to listen on
MCP_SERVER_PORT=8001

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
